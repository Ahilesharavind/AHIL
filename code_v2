import React, { useState, useEffect, useRef, useMemo } from 'react';
import { motion, AnimatePresence, useScroll, useSpring, useTransform } from 'framer-motion';
import { 
  Menu, X, ChevronRight, ArrowRight, Globe, BarChart3, 
  Code2, Camera, Search, Database, Settings, Users, 
  Mail, Phone, MapPin, Linkedin, Instagram, Twitter,
  Cpu, Zap, Award, Target, MessageCircle
} from 'lucide-react';

// --- CONFIGURATION & DATA ---
const BRAND_NAME = "AHIL DIGITAL";
const WHATSAPP_URL = "https://wa.me/919962194822";

const SERVICES = [
  { 
    id: "web-dev", 
    title: "Web Development", 
    icon: <Code2 className="w-6 h-6" />, 
    desc: "Architecting high-performance digital ecosystems with React & Next.js.",
    details: "Scalable, secure, and blazing fast web applications tailored for growth."
  },
  { 
    id: "meta-ads", 
    title: "Meta Ads", 
    icon: <Target className="w-6 h-6" />, 
    desc: "Precision targeting across Facebook and Instagram to drive ROI.",
    details: "Data-driven creative strategies that convert attention into revenue."
  },
  { 
    id: "google-ads", 
    title: "Google Ads", 
    icon: <Search className="w-6 h-6" />, 
    desc: "Dominating search results with strategic PPC campaigns.",
    details: "Capture high-intent traffic and maximize your conversion rates."
  },
  { 
    id: "software", 
    title: "Software Solutions", 
    icon: <Cpu className="w-6 h-6" />, 
    desc: "Custom enterprise software designed for complex workflows.",
    details: "From ERPs to specialized SaaS, we build tools that solve problems."
  },
  { 
    id: "creative", 
    title: "Ad Shoot & Production", 
    icon: <Camera className="w-6 h-6" />, 
    desc: "Cinema-grade visual storytelling for world-class brands.",
    details: "High-end video production, photography, and creative direction."
  },
  { 
    id: "data", 
    title: "Data Management", 
    icon: <Database className="w-6 h-6" />, 
    desc: "Turning raw data into actionable business intelligence.",
    details: "Secure warehousing, visualization, and predictive analytics."
  }
];

const JOB_OPENINGS = [
  { title: "Senior React Developer", type: "Full-time", location: "Remote / Hybrid" },
  { title: "Performance Marketer", type: "Full-time", location: "Mumbai / Remote" },
  { title: "Creative Director", type: "Contract", location: "Mumbai" }
];

// --- COMPONENTS ---

const CustomCursor = () => {
  const [position, setPosition] = useState({ x: 0, y: 0 });
  const [hovered, setHovered] = useState(false);

  useEffect(() => {
    const handleMouseMove = (e) => setPosition({ x: e.clientX, y: e.clientY });
    const handleMouseOver = (e) => {
      if (e.target.tagName === 'BUTTON' || e.target.tagName === 'A') setHovered(true);
      else setHovered(false);
    };
    window.addEventListener('mousemove', handleMouseMove);
    window.addEventListener('mouseover', handleMouseOver);
    return () => {
      window.removeEventListener('mousemove', handleMouseMove);
      window.removeEventListener('mouseover', handleMouseOver);
    };
  }, []);

  return (
    <motion.div
      className="fixed top-0 left-0 w-8 h-8 rounded-full border border-white pointer-events-none z-[9999] mix-blend-difference hidden md:block"
      animate={{
        x: position.x - 16,
        y: position.y - 16,
        scale: hovered ? 2.5 : 1,
        backgroundColor: hovered ? "rgba(255,255,255,1)" : "rgba(255,255,255,0)"
      }}
      transition={{ type: 'spring', damping: 20, stiffness: 250, mass: 0.5 }}
    />
  );
};

const Navbar = ({ activePage, setActivePage }) => {
  const [isScrolled, setIsScrolled] = useState(false);
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);

  useEffect(() => {
    const handleScroll = () => setIsScrolled(window.scrollY > 50);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const navLinks = [
    { id: 'home', label: 'Home' },
    { id: 'services', label: 'Services' },
    { id: 'about', label: 'About' },
    { id: 'careers', label: 'Careers' },
    { id: 'contact', label: 'Contact' }
  ];

  return (
    <nav className={`fixed top-0 w-full z-[1000] transition-all duration-500 ${isScrolled ? 'py-4' : 'py-8'}`}>
      <div className="container mx-auto px-6">
        <div className={`relative flex items-center justify-between px-6 py-3 rounded-full transition-all duration-500 ${isScrolled ? 'bg-black/60 backdrop-blur-xl border border-white/10 shadow-2xl' : 'bg-transparent'}`}>
          <button onClick={() => setActivePage('home')} className="text-xl font-black tracking-tighter text-white">
            {BRAND_NAME}
          </button>

          <div className="hidden md:flex items-center gap-8">
            {navLinks.map(link => (
              <button
                key={link.id}
                onClick={() => setActivePage(link.id)}
                className={`text-sm font-medium transition-all hover:text-white ${activePage === link.id ? 'text-white' : 'text-white/50'}`}
              >
                {link.label}
              </button>
            ))}
          </div>

          <div className="hidden md:flex items-center gap-4">
            <a href={WHATSAPP_URL} target="_blank" rel="noopener noreferrer" className="bg-white text-black px-6 py-2 rounded-full text-sm font-bold hover:bg-green-400 transition-all">
              WhatsApp Us
            </a>
          </div>

          <button className="md:hidden text-white" onClick={() => setMobileMenuOpen(true)}>
            <Menu className="w-6 h-6" />
          </button>
        </div>
      </div>

      <AnimatePresence>
        {mobileMenuOpen && (
          <motion.div 
            initial={{ opacity: 0, x: '100%' }}
            animate={{ opacity: 1, x: 0 }}
            exit={{ opacity: 0, x: '100%' }}
            className="fixed inset-0 bg-black z-[2000] flex flex-col p-12"
          >
            <div className="flex justify-end">
              <button onClick={() => setMobileMenuOpen(false)}><X className="w-8 h-8 text-white" /></button>
            </div>
            <div className="flex flex-col gap-8 mt-12">
              {navLinks.map(link => (
                <button
                  key={link.id}
                  onClick={() => { setActivePage(link.id); setMobileMenuOpen(false); }}
                  className="text-4xl font-bold text-white text-left hover:text-white/50"
                >
                  {link.label}
                </button>
              ))}
              <a href={WHATSAPP_URL} target="_blank" rel="noopener noreferrer" className="text-4xl font-bold text-green-400">
                WhatsApp Us
              </a>
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </nav>
  );
};

const SectionHeading = ({ subtitle, title, description, light = false }) => (
  <div className="mb-20">
    <motion.span 
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      className="inline-block px-4 py-1 rounded-full border border-white/10 text-xs font-bold tracking-widest uppercase mb-6 bg-white/5 text-blue-400"
    >
      {subtitle}
    </motion.span>
    <motion.h2 
      initial={{ opacity: 0, y: 20 }}
      whileInView={{ opacity: 1, y: 0 }}
      viewport={{ once: true }}
      transition={{ delay: 0.1 }}
      className={`text-4xl md:text-6xl font-bold tracking-tight mb-8 ${light ? 'text-white' : 'text-white'}`}
    >
      {title}
    </motion.h2>
    {description && (
      <motion.p 
        initial={{ opacity: 0, y: 20 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true }}
        transition={{ delay: 0.2 }}
        className="text-xl text-white/50 max-w-2xl leading-relaxed"
      >
        {description}
      </motion.p>
    )}
  </div>
);

// --- PAGES ---

const HomePage = ({ setActivePage }) => {
  const containerRef = useRef(null);
  const { scrollYProgress } = useScroll({ target: containerRef });
  const scale = useTransform(scrollYProgress, [0, 0.2], [1, 0.95]);
  const opacity = useTransform(scrollYProgress, [0, 0.2], [1, 0]);

  return (
    <div ref={containerRef} className="bg-black text-white">
      {/* Hero Section */}
      <section className="relative min-h-screen flex items-center justify-center overflow-hidden pt-32">
        <div className="absolute inset-0 z-0">
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[80vw] h-[80vw] bg-blue-600/20 blur-[120px] rounded-full animate-pulse" />
          <div className="absolute top-1/4 left-1/4 w-[40vw] h-[40vw] bg-purple-600/10 blur-[100px] rounded-full" />
        </div>

        <motion.div style={{ scale, opacity }} className="relative z-10 text-center px-6 max-w-7xl mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, ease: [0.16, 1, 0.3, 1] }}
          >
            <h1 className="text-5xl sm:text-6xl md:text-[110px] lg:text-[130px] font-black tracking-tighter leading-[0.85] mb-12">
              WE BUILD <br />
              <span className="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-400">GROWTH ENGINES</span>
            </h1>
          </motion.div>

          <motion.p
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.5, duration: 1 }}
            className="text-lg md:text-2xl text-white/60 max-w-3xl mx-auto mb-16 font-medium leading-relaxed"
          >
            Transforming ambitious brands through world-class software development and high-performance digital marketing.
          </motion.p>

          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.8 }}
            className="flex flex-col sm:flex-row gap-6 justify-center items-center"
          >
            <button 
              onClick={() => setActivePage('contact')}
              className="w-full sm:w-auto px-10 py-5 bg-white text-black rounded-full font-bold text-lg hover:scale-105 transition-transform active:scale-95 shadow-2xl shadow-white/10"
            >
              Start Your Revolution
            </button>
            <a 
              href={WHATSAPP_URL} 
              target="_blank" 
              rel="noopener noreferrer"
              className="w-full sm:w-auto px-10 py-5 bg-white/5 border border-white/10 text-white rounded-full font-bold text-lg backdrop-blur-md hover:bg-green-500/20 hover:border-green-400 transition-all flex items-center justify-center gap-2"
            >
              <MessageCircle className="w-5 h-5" /> WhatsApp Us
            </a>
          </motion.div>
        </motion.div>

        <motion.div 
          animate={{ y: [0, 10, 0] }}
          transition={{ duration: 2, repeat: Infinity }}
          className="absolute bottom-12 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2"
        >
          <span className="text-[10px] uppercase tracking-[0.3em] font-bold text-white/30">Scroll to Explore</span>
          <div className="w-px h-12 bg-gradient-to-b from-white/40 to-transparent" />
        </motion.div>
      </section>

      {/* Trust Section */}
      <section className="py-32 bg-black border-y border-white/5">
        <div className="container mx-auto px-6">
          <p className="text-center text-white/30 text-sm font-bold tracking-[0.3em] uppercase mb-16">Trusted by Forward-Thinking Brands</p>
          <div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-12 grayscale opacity-40 hover:grayscale-0 hover:opacity-100 transition-all duration-700">
            {[1,2,3,4,5,6].map(i => (
              <div key={i} className="h-12 flex items-center justify-center font-black text-2xl tracking-tighter">BRAND_{i}</div>
            ))}
          </div>
        </div>
      </section>

      {/* Services Highlight */}
      <section className="py-40 bg-[#050505]">
        <div className="container mx-auto px-6">
          <SectionHeading 
            subtitle="Expertise"
            title="Engineered for Impact"
            description="We combine technical precision with creative flair to deliver digital products that aren't just used—they're loved."
          />

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {SERVICES.slice(0, 3).map((s, i) => (
              <motion.div
                key={s.id}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: i * 0.1 }}
                whileHover={{ y: -10 }}
                className="group p-10 rounded-[40px] bg-white/[0.03] border border-white/5 hover:border-white/20 transition-all duration-500 cursor-pointer"
              >
                <div className="w-16 h-16 rounded-2xl bg-blue-600/20 flex items-center justify-center text-blue-400 mb-8 group-hover:scale-110 transition-transform">
                  {s.icon}
                </div>
                <h3 className="text-2xl font-bold mb-4">{s.title}</h3>
                <p className="text-white/50 leading-relaxed mb-8">{s.desc}</p>
                <button className="flex items-center gap-2 text-sm font-bold text-blue-400 opacity-0 group-hover:opacity-100 transition-opacity">
                  Learn More <ArrowRight className="w-4 h-4" />
                </button>
              </motion.div>
            ))}
          </div>

          <div className="mt-20 text-center">
            <button 
              onClick={() => setActivePage('services')}
              className="group inline-flex items-center gap-4 text-xl font-bold hover:text-blue-400 transition-colors"
            >
              View all 11 core services
              <div className="p-3 bg-white/5 rounded-full group-hover:bg-blue-600/20 transition-colors">
                <ChevronRight className="w-6 h-6" />
              </div>
            </button>
          </div>
        </div>
      </section>

      {/* Parallax Feature Section */}
      <section className="py-40 overflow-hidden relative">
         <div className="container mx-auto px-6">
            <div className="grid grid-cols-1 lg:grid-cols-2 gap-20 items-center">
              <div>
                <SectionHeading 
                  subtitle="Why AHIL Digital"
                  title="We don't just deliver. We dominate."
                  description="Standard agencies follow a brief. We become your strategic growth partners, obsessing over every line of code and every pixel in your ad."
                />
                <div className="space-y-8">
                  {[
                    { t: "Elite Talent", d: "Our team consists of senior engineers and award-winning marketers." },
                    { t: "Data Transparency", d: "Live dashboards so you see your ROI in real-time." },
                    { t: "Speed to Market", d: "Agile methodologies that cut development time by 40%." }
                  ].map((item, i) => (
                    <div key={i} className="flex gap-6">
                      <div className="mt-1 w-6 h-6 rounded-full bg-blue-600 flex-shrink-0" />
                      <div>
                        <h4 className="text-xl font-bold mb-2">{item.t}</h4>
                        <p className="text-white/40">{item.d}</p>
                      </div>
                    </div>
                  ))}
                </div>
              </div>
              <div className="relative">
                <div className="aspect-square bg-gradient-to-br from-blue-600/20 to-purple-600/20 rounded-[60px] p-1 border border-white/10 overflow-hidden shadow-2xl">
                  <div className="h-full w-full bg-black/40 backdrop-blur-3xl rounded-[58px] flex items-center justify-center relative overflow-hidden">
                    <motion.div 
                      animate={{ rotate: 360 }}
                      transition={{ duration: 20, repeat: Infinity, ease: 'linear' }}
                      className="absolute w-[150%] h-[150%] opacity-20"
                      style={{ background: 'conic-gradient(from 0deg, transparent, #3b82f6, transparent)' }}
                    />
                    <div className="relative z-10 text-center">
                      <div className="text-8xl font-black tracking-tighter mb-4">98%</div>
                      <div className="text-xl text-white/40 font-bold uppercase tracking-widest">Client Retention</div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
         </div>
      </section>

      {/* CTA Section */}
      <section className="py-40">
        <div className="container mx-auto px-6">
          <div className="bg-gradient-to-r from-blue-600 to-indigo-800 rounded-[60px] p-12 md:p-24 relative overflow-hidden">
            <div className="absolute top-0 right-0 w-[40vw] h-[40vw] bg-white/10 blur-[100px] rounded-full -translate-y-1/2 translate-x-1/2" />
            <div className="relative z-10 max-w-3xl">
              <h2 className="text-5xl md:text-7xl font-bold tracking-tight mb-8">Ready to scale beyond the ordinary?</h2>
              <p className="text-xl text-white/80 mb-12 max-w-xl">Join 50+ world-class brands that have achieved exponential growth with our digital strategies.</p>
              <div className="flex flex-wrap gap-6">
                <button 
                  onClick={() => setActivePage('contact')}
                  className="px-12 py-6 bg-white text-black rounded-full font-bold text-xl hover:scale-105 transition-transform shadow-2xl shadow-black/20"
                >
                  Let's Talk
                </button>
                <a 
                  href={WHATSAPP_URL}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="px-12 py-6 bg-[#25D366] text-white rounded-full font-bold text-xl hover:scale-105 transition-transform shadow-2xl shadow-green-900/20 flex items-center gap-3"
                >
                  <MessageCircle className="w-6 h-6 fill-white" /> WhatsApp Us
                </a>
              </div>
            </div>
          </div>
        </div>
      </section>
    </div>
  );
};

// ... [ServicesPage, AboutPage, CareersPage components remain largely the same]

const ServicesPage = () => (
  <div className="pt-40 pb-40 bg-black min-h-screen">
    <div className="container mx-auto px-6">
      <SectionHeading 
        subtitle="Services Portfolio"
        title="Comprehensive Digital Solutions"
        description="We offer an end-to-end ecosystem of services designed to take your brand from inception to market leader."
      />
      
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {[...SERVICES, { title: "SEO Optimization", desc: "Long-term organic visibility strategy.", icon: <Search className="w-6 h-6" /> }, 
        { title: "CRM Management", desc: "Automate your customer lifecycle.", icon: <Users className="w-6 h-6" /> },
        { title: "Outsourcing", desc: "High-quality engineering teams on demand.", icon: <Settings className="w-6 h-6" /> }].map((s, i) => (
          <motion.div 
            key={i}
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            viewport={{ once: true }}
            className="p-12 rounded-[40px] bg-white/[0.03] border border-white/5 group hover:bg-white/[0.05] transition-all"
          >
            <div className="w-14 h-14 rounded-2xl bg-blue-600/20 flex items-center justify-center text-blue-400 mb-8">{s.icon}</div>
            <h3 className="text-2xl font-bold mb-4">{s.title}</h3>
            <p className="text-white/40 mb-8 leading-relaxed">{s.desc}</p>
            <div className="h-px w-full bg-white/5 mb-8" />
            <ul className="space-y-3">
              {['Market Analysis', 'Tech Implementation', 'Ongoing Support'].map((item, idx) => (
                <li key={idx} className="flex items-center gap-3 text-sm text-white/60">
                  <div className="w-1.5 h-1.5 rounded-full bg-blue-500" />
                  {item}
                </li>
              ))}
            </ul>
          </motion.div>
        ))}
      </div>
    </div>
  </div>
);

const AboutPage = () => (
  <div className="pt-40 pb-40 bg-black">
    <div className="container mx-auto px-6">
      <SectionHeading 
        subtitle="Our DNA"
        title="We are AHIL Digital."
        description="Born from a passion for technology and a relentless drive for performance, we help brands navigate the digital landscape with confidence."
      />

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-24 mt-20">
        <div className="space-y-12">
          <div>
            <h3 className="text-2xl font-bold mb-4">Our Mission</h3>
            <p className="text-white/50 text-lg leading-relaxed">To bridge the gap between human creativity and technological capability, creating digital experiences that inspire and convert.</p>
          </div>
          <div>
            <h3 className="text-2xl font-bold mb-4">Our Vision</h3>
            <p className="text-white/50 text-lg leading-relaxed">To be the global benchmark for digital transformation, empowering 1,000+ businesses to thrive in the decentralized digital future.</p>
          </div>
          <div className="flex gap-12 pt-8">
            <div>
              <div className="text-4xl font-bold mb-1">5YRS+</div>
              <div className="text-sm text-white/30 uppercase tracking-widest font-bold">Experience</div>
            </div>
            <div>
              <div className="text-4xl font-bold mb-1">200+</div>
              <div className="text-sm text-white/30 uppercase tracking-widest font-bold">Deployments</div>
            </div>
          </div>
        </div>

        <div className="bg-white/5 rounded-[60px] p-12 border border-white/5 flex flex-col justify-center">
          <h3 className="text-3xl font-bold mb-8 italic">"Innovation is not about doing something new, it's about doing what matters in a new way."</h3>
          <div className="flex items-center gap-6">
            <div className="w-16 h-16 rounded-full bg-gradient-to-br from-blue-500 to-purple-500" />
            <div>
              <div className="font-bold text-lg">Founding Director</div>
              <div className="text-white/40 uppercase text-xs tracking-widest font-bold">AHIL Digital</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
);

const CareersPage = () => (
  <div className="pt-40 pb-40 bg-black">
    <div className="container mx-auto px-6">
      <div className="text-center mb-32">
        <motion.span 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          className="text-blue-500 font-bold tracking-[0.3em] uppercase block mb-6"
        >
          Join the Elite
        </motion.span>
        <h2 className="text-6xl md:text-8xl font-bold tracking-tighter mb-12">Shape the Future.</h2>
        <p className="text-xl text-white/50 max-w-2xl mx-auto">We are always looking for visionary developers, marketers, and creatives who refuse to settle for "good enough".</p>
      </div>

      <div className="space-y-6 max-w-4xl mx-auto">
        {JOB_OPENINGS.map((job, i) => (
          <motion.div 
            key={i}
            whileHover={{ x: 20 }}
            className="p-8 rounded-3xl bg-white/5 border border-white/5 flex flex-col md:flex-row md:items-center justify-between group cursor-pointer hover:bg-white/10 transition-all"
          >
            <div>
              <h3 className="text-2xl font-bold mb-2 group-hover:text-blue-400 transition-colors">{job.title}</h3>
              <div className="flex gap-6 text-sm text-white/40 font-medium">
                <span>{job.type}</span>
                <span>•</span>
                <span>{job.location}</span>
              </div>
            </div>
            <button className="mt-6 md:mt-0 flex items-center gap-4 font-bold text-sm uppercase tracking-widest group-hover:gap-6 transition-all">
              Apply Now <ChevronRight className="w-5 h-5 text-blue-500" />
            </button>
          </motion.div>
        ))}
      </div>
    </div>
  </div>
);

const ContactPage = () => (
  <div className="pt-40 pb-40 bg-black min-h-screen">
    <div className="container mx-auto px-6">
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-24">
        <div>
          <SectionHeading 
            subtitle="Contact"
            title="Start your evolution today."
            description="Whether you have a specific project in mind or just want to explore the possibilities, we're ready to listen."
          />
          
          <div className="space-y-12 mt-12">
            <div className="flex items-center gap-8">
              <div className="w-14 h-14 rounded-full bg-white/5 flex items-center justify-center text-blue-400"><Mail /></div>
              <div>
                <p className="text-sm text-white/30 font-bold uppercase tracking-widest mb-1">Email us</p>
                <p className="text-xl font-bold">hello@ahildigital.in</p>
              </div>
            </div>
            <div className="flex items-center gap-8 group cursor-pointer">
              <div className="w-14 h-14 rounded-full bg-white/5 flex items-center justify-center text-green-400 group-hover:bg-green-500/20 transition-all"><MessageCircle /></div>
              <a href={WHATSAPP_URL} target="_blank" rel="noopener noreferrer">
                <p className="text-sm text-white/30 font-bold uppercase tracking-widest mb-1">WhatsApp us</p>
                <p className="text-xl font-bold">+91 99621 94822</p>
              </a>
            </div>
            <div className="flex items-center gap-8">
              <div className="w-14 h-14 rounded-full bg-white/5 flex items-center justify-center text-blue-400"><MapPin /></div>
              <div>
                <p className="text-sm text-white/30 font-bold uppercase tracking-widest mb-1">Headquarters</p>
                <p className="text-xl font-bold">BKC, Mumbai, India</p>
              </div>
            </div>
          </div>
        </div>

        <div className="bg-white/5 p-12 rounded-[50px] border border-white/5">
          <form className="space-y-8" onSubmit={(e) => e.preventDefault()}>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
              <div className="space-y-2">
                <label className="text-xs font-bold text-white/40 uppercase tracking-widest ml-4">Name</label>
                <input type="text" className="w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl focus:border-blue-500 outline-none transition-colors" placeholder="John Doe" />
              </div>
              <div className="space-y-2">
                <label className="text-xs font-bold text-white/40 uppercase tracking-widest ml-4">Email</label>
                <input type="email" className="w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl focus:border-blue-500 outline-none transition-colors" placeholder="john@example.com" />
              </div>
            </div>
            <div className="space-y-2">
              <label className="text-xs font-bold text-white/40 uppercase tracking-widest ml-4">Company</label>
              <input type="text" className="w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl focus:border-blue-500 outline-none transition-colors" placeholder="Acme Inc." />
            </div>
            <div className="space-y-2">
              <label className="text-xs font-bold text-white/40 uppercase tracking-widest ml-4">Message</label>
              <textarea className="w-full px-6 py-4 bg-white/5 border border-white/10 rounded-2xl focus:border-blue-500 outline-none transition-colors min-h-[150px]" placeholder="Tell us about your project..." />
            </div>
            <button className="w-full py-5 bg-white text-black rounded-full font-bold text-lg hover:bg-blue-400 hover:text-white transition-all transform active:scale-95">Send Inquiry</button>
          </form>
        </div>
      </div>
    </div>
  </div>
);

const Footer = ({ setActivePage }) => (
  <footer className="bg-black border-t border-white/5 py-24 pb-12">
    <div className="container mx-auto px-6">
      <div className="grid grid-cols-1 md:grid-cols-4 gap-12 mb-24">
        <div className="col-span-1 md:col-span-1">
          <div className="text-3xl font-black tracking-tighter mb-8">{BRAND_NAME}.</div>
          <p className="text-white/40 leading-relaxed mb-8">Architecting the digital future, one line of code at a time.</p>
          <div className="flex gap-6">
            <Linkedin className="w-5 h-5 text-white/40 hover:text-white cursor-pointer transition-colors" />
            <Instagram className="w-5 h-5 text-white/40 hover:text-white cursor-pointer transition-colors" />
            <Twitter className="w-5 h-5 text-white/40 hover:text-white cursor-pointer transition-colors" />
          </div>
        </div>
        
        {[
          { title: "Solutions", links: ["Web Development", "Performance Ads", "Creative Studio", "Consulting"] },
          { title: "Company", links: ["About Us", "Careers", "Culture", "Blog"] },
          { title: "Support", links: ["Contact", "Privacy Policy", "Terms of Use", "FAQ"] }
        ].map((col, i) => (
          <div key={i}>
            <h4 className="text-sm font-bold uppercase tracking-[0.2em] mb-8">{col.title}</h4>
            <ul className="space-y-4">
              {col.links.map((link, idx) => (
                <li key={idx}><button className="text-white/40 hover:text-white transition-colors">{link}</button></li>
              ))}
            </ul>
          </div>
        ))}
      </div>
      
      <div className="flex flex-col md:flex-row justify-between items-center border-t border-white/5 pt-12 text-sm text-white/20 font-medium">
        <p>© 2024 {BRAND_NAME}. All rights reserved.</p>
        <p className="mt-4 md:mt-0 tracking-[0.3em] uppercase">Built for Performance</p>
      </div>
    </div>
  </footer>
);

const FloatingWhatsApp = () => (
  <motion.a
    href={WHATSAPP_URL}
    target="_blank"
    rel="noopener noreferrer"
    whileHover={{ scale: 1.1 }}
    whileTap={{ scale: 0.9 }}
    className="fixed bottom-10 right-10 w-16 h-16 bg-[#25D366] rounded-full flex items-center justify-center shadow-2xl z-[999] cursor-pointer"
  >
    <MessageCircle className="w-8 h-8 text-white fill-current" />
  </motion.a>
);

const App = () => {
  const [activePage, setActivePage] = useState('home');
  const [loading, setLoading] = useState(true);

  const { scrollYProgress } = useScroll();
  const scaleX = useSpring(scrollYProgress, { stiffness: 100, damping: 30, restDelta: 0.001 });

  useEffect(() => {
    const timer = setTimeout(() => setLoading(false), 2000);
    return () => clearTimeout(timer);
  }, []);

  useEffect(() => {
    window.scrollTo(0, 0);
  }, [activePage]);

  if (loading) {
    return (
      <div className="fixed inset-0 bg-black flex flex-col items-center justify-center z-[9999]">
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          className="text-2xl font-black tracking-tighter text-white mb-6"
        >
          {BRAND_NAME}.
        </motion.div>
        <div className="w-48 h-1 bg-white/5 rounded-full overflow-hidden">
          <motion.div 
            initial={{ x: '-100%' }}
            animate={{ x: '100%' }}
            transition={{ duration: 1.5, repeat: Infinity, ease: 'easeInOut' }}
            className="w-full h-full bg-blue-600"
          />
        </div>
      </div>
    );
  }

  return (
    <div className="bg-black text-white selection:bg-blue-500 selection:text-white">
      <CustomCursor />
      <motion.div 
        className="fixed top-0 left-0 right-0 h-1 bg-blue-600 origin-left z-[2000]"
        style={{ scaleX }}
      />
      
      <Navbar activePage={activePage} setActivePage={setActivePage} />
      
      <main className="transition-all duration-500">
        {activePage === 'home' && <HomePage setActivePage={setActivePage} />}
        {activePage === 'services' && <ServicesPage />}
        {activePage === 'about' && <AboutPage />}
        {activePage === 'careers' && <CareersPage />}
        {activePage === 'contact' && <ContactPage />}
      </main>

      <Footer setActivePage={setActivePage} />
      <FloatingWhatsApp />
    </div>
  );
};

export default App;
